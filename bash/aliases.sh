# SSH
alias pssh="ssh -l prodsoft "
# ACK-GREP
alias agrep="ack-grep -i --ignore-dir=.build "
# DZIL INSTALL
alias dzinst="dzil install --install-command='wcpanm -n .'"
# git pull origin devel
alias gpod="git pull origin devel"
# git push origin users/ejoffri
alias gpou="git push origin users/ejoffri"
